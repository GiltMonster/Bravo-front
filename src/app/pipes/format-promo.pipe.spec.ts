import { FormatPromoPipe } from './format-promo.pipe';

describe('FormatPromoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPromoPipe();
    expect(pipe).toBeTruthy();
  });
});
